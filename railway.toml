# Railway configuration for the MCP server service.
# This is the primary service — always running, serving MCP over SSE.
#
# The auto-update cron service uses a separate Railway service in the same project.
# See configs/railway-updater.toml for its configuration.

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 10
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
